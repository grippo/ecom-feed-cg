{
  "name": "@ecom-feed/core",
  "version": "0.1.0",
  "description": "W3C E-Com Product Feed – Core Schema & Validator",
  "bin": {
    "ecom-feed-validate": "./dist/validator.js"
  },
  "scripts": {
    "build": "mkdir -p dist && cp validator.js dist/",
    "test": "node test-suite/run.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["ecommerce", "product-feed", "json-ld", "schema.org"],
  "author": "W3C E-Com Feed CG",
  "license": "MIT",
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  }
}
